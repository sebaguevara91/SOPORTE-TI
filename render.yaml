services:
  - type: web
    name: soporte-ti-visitas
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/server.py
    plan: free
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
